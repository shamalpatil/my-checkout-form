import { OppIfDirective } from './opp-if.directive';

describe('OppIfDirective', () => {
  it('should create an instance', () => {
    const directive = new OppIfDirective();
    expect(directive).toBeTruthy();
  });
});
